on:
  workflow_dispatch:
jobs:
  check-dispatc-version:
    runs-on: self-hosted
    steps:
      - name: Run a one-line script
        run: echo "This is workflow_dispatch jobs 20240710"
      - run: |
          cd C:\\actions-runner
          echo "hello world"
          
