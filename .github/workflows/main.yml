on:
  workflow_dispatch:
jobs:
  check-bats-version:
    runs-on: self-hosted
    steps:
      - name: Run a one-line script
      - run: |
          cd C:\\actions-runner
          echo "hello world"
          
